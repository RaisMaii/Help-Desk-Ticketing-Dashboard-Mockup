{
  "name": "helpdesk-ticketing-simulation",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"node server/server.js\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "seed": "node server/seed.js",
    "install-all": "npm install && npm install --prefix server && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
